import { KlarnaCheckout } from "./index";

describe("klarna-checkout-typescript-sdk", () => {
    it("initialize client", async () => {
        const klarnacheckout = new KlarnaCheckout({
        });
    });
});
